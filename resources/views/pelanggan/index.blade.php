@extends('layouts.app') @section('title','Data Pelanggan') @section('content')<div class="container py-5"><div class="d-flex justify-content-between mb-3"><h2 class="fw-bold">Data Pelanggan</h2><a class="btn btn-primary" href="{{ route('pelanggan.create') }}">+ Tambah Pelanggan</a></div><form class="mb-3"><div class="input-group"><input name="cari" class="form-control" placeholder="Cari nama, HP, atau KTP..." value="{{ request('cari') }}"><button class="btn btn-dark">Cari</button></div></form><div class="card p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>No</th><th>Nama</th><th>No. HP</th><th>No. KTP</th><th>Alamat</th><th>Aksi</th></tr></thead><tbody>@forelse($data as $p)<tr><td>{{ $data->firstItem()+$loop->index }}</td><td>{{ $p->nama }}</td><td>{{ $p->no_hp }}</td><td>{{ $p->no_ktp }}</td><td>{{ $p->alamat }}</td><td><a class="btn btn-sm btn-warning" href="{{ route('pelanggan.edit',$p) }}">Edit</a><form class="d-inline hapus" method="POST" action="{{ route('pelanggan.destroy',$p) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form></td></tr>@empty<tr><td colspan="6" class="text-center">Belum ada data.</td></tr>@endforelse</tbody></table></div>{{ $data->links() }}</div></div>@endsection
