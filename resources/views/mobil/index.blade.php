@extends('layouts.app') @section('title','Data Mobil') @section('content')<div class="container py-5"><div class="d-flex justify-content-between mb-3"><h2 class="fw-bold">Data Mobil</h2><a class="btn btn-primary" href="{{ route('mobil.create') }}">+ Tambah Mobil</a></div><form class="mb-3"><div class="input-group"><input name="cari" class="form-control" placeholder="Cari nama, merk, atau plat..." value="{{ request('cari') }}"><button class="btn btn-dark">Cari</button></div></form><div class="card p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>No</th><th>Mobil</th><th>Merk</th><th>Tahun</th><th>Plat</th><th>Harga/Hari</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($data as $m)<tr><td>{{ $data->firstItem()+$loop->index }}</td><td>{{ $m->nama_mobil }}</td><td>{{ $m->merk }}</td><td>{{ $m->tahun }}</td><td>{{ $m->plat_nomor }}</td><td>Rp {{ number_format($m->harga_sewa,0,',','.') }}</td><td><span class="badge bg-{{ $m->status=='Tersedia'?'success':($m->status=='Disewa'?'warning':'secondary') }}">{{ $m->status }}</span></td><td><a class="btn btn-sm btn-warning" href="{{ route('mobil.edit',$m) }}">Edit</a><form class="d-inline hapus" method="POST" action="{{ route('mobil.destroy',$m) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Hapus</button></form></td></tr>@empty<tr><td colspan="8" class="text-center">Belum ada data.</td></tr>@endforelse</tbody></table></div>{{ $data->links() }}</div></div>@endsection
